```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash
```
```bash
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
```
```bash
nvm install 24
```
```bash
node -v
```

```bash
npm install @agent-tars/cli@latest -g
npm install @agent-tars/cli@0.2.10 -g
```